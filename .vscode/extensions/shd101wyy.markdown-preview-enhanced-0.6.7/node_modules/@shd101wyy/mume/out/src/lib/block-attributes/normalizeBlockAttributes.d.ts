import { BlockAttributes } from "./types";
/**
 * Walks through attribute keys and makes them snakeCase if needed
 * @param attributes
 */
export declare const normalizeBlockAttributes: (attributes: BlockAttributes) => BlockAttributes;
