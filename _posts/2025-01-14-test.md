---
title: 'Test'
date: 2025-01-14
permalink: /posts/2025/01/test/
tags:
  - Algo
---

<div style="text-align: justify">
<br>
test
</div>

```R
# Generate a dynamic seed based on the current timestamp
seed <- as.numeric(Sys.time()) %% 1e6  # Use the last 6 digits of the timestamp
set.seed(seed)  # Set the seed for reproducibility

# Save the seed to ensure reproducibility
cat("Semilla utilizada:", seed, "\n")

# List of candidate names in the given order
candidate_names <- c(
  "Jairala", "Escala", "AGonzalez", "Araus", 
  "LGonzalez", "Kronfle", "Noboa", "Tilleria", 
  "Rabascall", "Cueva", "Granja", "Iza", 
  "Saquicela", "Tabacchi", "Gomez", "Cucalon"
)

# Shuffle the candidates randomly
shuffled_candidates <- sample(candidate_names)

# Divide into two groups
group_1 <- shuffled_candidates[1:8]  # First group (8 candidates)
group_2 <- shuffled_candidates[9:16] # Second group (8 remaining candidates)

# Display the results with candidate names
cat("Grupo 1:", paste(group_1, collapse = ", "), "\n")
cat("Grupo 2:", paste(group_2, collapse = ", "), "\n")